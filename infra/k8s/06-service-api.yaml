apiVersion: v1
kind: Service
metadata:
  name: rigguard-api
  namespace: rigguard
  labels:
    app: rigguard-api
spec:
  selector:
    app: rigguard-api
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
